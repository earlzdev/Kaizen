# Pytest config (Step 0). pythonpath=. makes `agents/`, `brain/`, `tools/`
# importable without packaging; asyncio_mode=auto lets async tests run without
# per-test decorators.
[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
